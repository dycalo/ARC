# ARC CLI

The `arc` command ships in `@dycalo/arc`. Start with the [harness guide](../../docs/harness.md) for `arc setup`, `arc web`, and `arc exec`.

The [standalone runner guide](../../docs/cli.md) documents `arc init`, `arc run`, resume, file tools, and contract administration. See [configuration](../../docs/configuration.md) for context and memory settings.
